const habitsRouter = require("express").Router();

// TODO

// Sriram
habitsRouter.get("/",/*fill here*/, async (req, res) => {
    let page = parseInt(req.query.page)
})
module.exports = habitsRouter;
